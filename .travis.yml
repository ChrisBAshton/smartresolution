language: php
php:
  - 5.4
install:
  - composer install
script:
  # create our databases
  - sqlite3 data/production.db < data/db.sql 
  - sqlite3 data/test.db < data/db.sql 
  # populate the test database
  - php test/seed.php
  # run our tests. @TODO - this obviously needs expanding to cover more areas
  - ./vendor/phpunit/phpunit/phpunit test/unit_tests/User.php 