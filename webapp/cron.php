<?php

// $lifespans = array();
// $disputeIds= Database::instance()->exec('SELECT dispute_id FROM disputes ORDER BY dispute_id DESC');

// foreach($disputeIds as $disputeID) {
//     array_push($lifespans, LifespanFactory::getCurrentLifespan((int) $disputeID['dispute_id']));
// }

// foreach($lifespans as $lifespan) {
//     // if lifespan has just started or ended
//     // create a notification and send to all parties
// }
